module.exports = (req, res, next) => {
    // const _id = req.params.prod_id;
    // const errors = { errors: [] };

    // const { name, description, imageUrl } = req.body;
    // if (!name || !description || !imageUrl ) {
    //     errors.errors.push({ message: 'All fields are required!' });
    // }
    // req.errors = errors;
    next();
}